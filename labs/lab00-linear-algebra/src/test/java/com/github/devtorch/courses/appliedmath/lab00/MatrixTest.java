package com.github.devtorch.courses.appliedmath.lab00;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MatrixTest {

/*    @Test void mulVector() {
        Matrix A = new DenseMatrix(new double[][]{
                {1, 2, 0},
                {0, 1, -1}
        });
        Vector x = Vector.of(1, 1, 1);
        Vector y = A.mul(x);
        assertNotNull(y);
        assertArrayEquals(new double[]{3, 0}, y.toArray(), 1e-9);
    }

    @Test void transpose() {
        Matrix A = new DenseMatrix(new double[][]{ {0, 1}, {2, 3} });
        Matrix AT = A.T();
        assertNotNull(AT);
        assertEquals(0, AT.at(0,0));
        assertEquals(2, AT.at(1,0));
        assertEquals(1, AT.at(0,1));
    }*/
}
