package com.github.devtorch.courses.appliedmath.lab00;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class VectorTest {

/*    @Test void addAndNorm() {
        Vector v = Vector.of(1, 2, 3);
        Vector w = Vector.of(3, 2, 1);
        Vector s = v.add(w);
        assertArrayEquals(new double[]{4, 4, 4}, s.toArray(), 1e-9);
        assertEquals( Math.sqrt(48), s.norm(), 1e-9 );
    }

    @Test void dotProduct() {
        Vector v = Vector.of(1, 0, -1);
        Vector w = Vector.of(-1, 0,  1);
        assertEquals(-2, v.dot(w));
    }*/
}
